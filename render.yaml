services:
  - type: web
    name: fashionsmart-backend
    env: python
    plan: free
    buildCommand: echo "No dependencies to install"
    startCommand: python single_endpoint_server.py
    envVars:
      - key: PORT
        value: 5000