services:
  - type: web
    name: fashionsmart-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python flask_root_server.py
    envVars:
      - key: PORT
        value: 5000